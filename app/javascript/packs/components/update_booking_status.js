class UpdateBookingStatus {
  constructor() {
  }

  bindEvents() {
    // Please add update button right next to the Status tab and when user clicks "更新" button, changed status will be updated.
    // https://app.clickup.com/t/1qu0ura
    // $('.update-status-input').on('change', (e) => {
    //   $(e.target).closest('form').submit();
    // })
  }
}

export default UpdateBookingStatus
